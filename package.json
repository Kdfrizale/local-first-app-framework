{
  "name": "local-first-framework",
  "version": "1.0.0",
  "description": "Simple local-first application framework with GitHub sync",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  },
  "devDependencies": {
    "@vitest/ui": "^1.6.0",
    "happy-dom": "^14.12.0",
    "vitest": "^1.6.0"
  }
}
